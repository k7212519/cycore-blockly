import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class CompileValidationService {
  triggerAfterSuccessfulCompile(): void {
    // Invitation validation belongs to the retired cloud account system.
  }
}
